<% stringToDate = function(s) {
    var arr = s.split(' ');
    var day = arr[0];
    var mon = arr[1];
    var date = arr[2];
    var year = arr[3];
    return day + ", " + date + " " + mon + " " + year;
} %>